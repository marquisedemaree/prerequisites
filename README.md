# Prerequisites
Installation instructions for projects requiring Python 3.10 and newer.
Copy code snippets and run from the Terminal.

## 1. Install a package manager
  - Mac/Linux: ```/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"```
